#!/bin/sh
# vim: set ts=4 sw=4:
set -eu

if [ -z "${TIMEZONE:-}" ]; then
	exit 0
fi

timezone=$(cat /etc/timezone) || true

if [ "$timezone" = "$TIMEZONE" ]; then
	exit 0
fi

echo "$TIMEZONE" > /etc/timezone
emerge --config sys-libs/timezone-data
